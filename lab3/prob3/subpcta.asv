% subpct a
x = zeros(1, 6);
y = zeros(1, 6);
for n = 10:15
    k = 1:n;
    t = -1 + 2 * k / n;
    t1 = zeros(1, n);
    for i = 1:n
        t(i) 
    
    v = vander(t);
    c = cond(v, 'inf')
    
    x(n - 9) = n;
    y(n - 9) = c;
end

scatter(x, y)