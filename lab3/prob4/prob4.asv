n = 4; k = 2;
a = poly(1:n);
a = a(2:end);

sum = 0;
for i=0:n-1
    sum = sum + abs(a(i+1)) * abs(k)^i;
end
p = 1;
for i=0:n-1
    % ~=  -> diferit
    if i ~= k
        p = p * (k-i);
    end
end
abs(p * k)

 p = 1;
for i = 1:n
    if i ~= k
        p = p * (k - i);
    else
        p = p * (-k);
    end
end
abs(p * k)

r = [1:k-1,k+1:n];
prod(k - r)